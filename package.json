{
  "name": "pc-configurator-comparer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api:dev": "python -m uvicorn main:app --reload --app-dir backend",
    "api:start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000 --app-dir backend"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^7.1.9"
  }
}